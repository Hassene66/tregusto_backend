<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <!-- =============================== -->
    <!-- Create newsletter subscriptions table -->
    <!-- =============================== -->
    <changeSet id="newsletter-subscriptions-001" author="Hassene">
        <createTable tableName="newsletter_subscriptions">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="email" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(20)" defaultValue="PENDING">
                <constraints nullable="false"/>
            </column>

            <column name="confirmation_token" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="confirmed_at" type="TIMESTAMP"/>
            <column name="unsubscribed_at" type="TIMESTAMP"/>

            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addUniqueConstraint tableName="newsletter_subscriptions" columnNames="email"/>

        <createIndex indexName="idx_newsletter_email" tableName="newsletter_subscriptions">
            <column name="email"/>
        </createIndex>

        <createIndex indexName="idx_newsletter_status" tableName="newsletter_subscriptions">
            <column name="status"/>
        </createIndex>

        <createIndex indexName="idx_newsletter_confirmation_token" tableName="newsletter_subscriptions">
            <column name="confirmation_token"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
